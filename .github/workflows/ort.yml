name: Run ORT
on:
  pull_request:
    branches:
      - '**'

jobs:
  call-ort:
    uses: JH-OSPO/org-actions/.github/workflows/ort-base.yml@main
    with:
      repo-path: .
      ort-output: ort-results
