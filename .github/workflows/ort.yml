name: Run ORT
on:
  pull_request:
    branches:
      - '**'

jobs:
  call-ort:
    uses: org-name/org-actions/.github/workflows/ort-reusable.yml@main
    with:
      repo-path: .
      ort-output: ort-results
