name: Run ORT
on:
  pull_request:
    branches:
      - 'main'
      - 'test-ort'
jobs:
  call-ort:
    uses: ./.github/workflows/ort-base.yml
    with:
      repo-path: .
      ort-output: ort-results