name: "Run ScanCode"

on:
  push:
    branches: [main]
  pull_request:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - name: Scan with ScanCode
        uses: JH-OSPO/org-actions@v1
        with:
          scan-path: "."
          output-name: "scan-results"
